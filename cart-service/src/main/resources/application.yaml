spring:
  application:
    name: cart-service

  profiles:
    active: dev

  jpa:
    open-in-view: false

server:
  port: 8083

security:
  jwt:
    secret: very-secret-key-very-secret-key-very-secret
