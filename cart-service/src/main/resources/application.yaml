spring:
  application:
    name: cart-service

  profiles:
    active: dev

  jpa:
    open-in-view: false

  flyway:
    enabled: true

server:
  port: 8083

security:
  jwt:
    secret: super-secret-key-super-secret-key
